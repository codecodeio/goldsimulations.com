---
import Button from "@/components/button/Button.astro";
import { getLocaleFromUrl } from "@/js/localeUtils";
import { getTranslatedData, useTranslations } from "@/js/translationUtils";

const currLocale = getLocaleFromUrl(Astro.url);
const siteData = getTranslatedData("siteData", currLocale);
const t = useTranslations(currLocale);
---

<section class="site-container flex flex-col pt-24 md:pt-32">
	<h1 class="text-3xl font-medium text-pretty md:text-5xl" data-aos="fade-right">
		{t("hero_text")}
	</h1>
	<div class="mt-10 flex justify-between gap-10">
		<p class="text-muted-foreground max-w-xl" data-aos="fade-right">
			{siteData.title}
		</p>
		<Button
			href="https://cosmicthemes.com/all-access/"
			target="_blank"
			rel="noreferrer"
			variant="primary"
			class="mt-auto"
			data-aos="fade-left"
			data-aos-delay=".6"
		>
			Get All Access
		</Button>
	</div>
</section>
